using System;

class Pow {
    public static void Main() {
        int _base = 2;
        int _exponent = 3;
        int _result = (int) Math.Pow(_base, _exponent);
        Console.WriteLine(_result);
    }
}